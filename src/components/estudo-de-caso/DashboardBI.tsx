// This component is no longer used in the current report structure.
// Kept as placeholder to avoid import errors if referenced elsewhere.

const DashboardBI = () => null;
export default DashboardBI;
